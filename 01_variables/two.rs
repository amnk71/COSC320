fn main(){ 
    // TODO: Change the line below to fix the compiler error.
    let x: i32=40;//"i32" represents a 32 bit integer
   
    if x == 10 {
        println!("x is ten!");
    } else {
        println!("x is not ten!");
    }
    }